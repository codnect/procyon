package component

import "math"

// HighestPriority and LowestPriority are constants that represent the highest and lowest possible priorities.
const (
	// HighestPriority is the highest possible priority.
	HighestPriority = math.MinInt
	// LowestPriority is the lowest possible priority.
	LowestPriority = math.MaxInt
)
